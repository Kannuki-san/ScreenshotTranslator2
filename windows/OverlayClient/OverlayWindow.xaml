<Window x:Class="OverlayClient.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Grid x:Name="Root" Background="Transparent">
        <Border x:Name="ChromeBorder" CornerRadius="6" Background="#AA000000">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="HeaderRow" Height="28"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel x:Name="HeaderPanel" Grid.Row="0" Margin="6,4,6,4" LastChildFill="True">
                    <TextBlock x:Name="StatusText" Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CopyButton" Content="Copy" Margin="0,0,6,0" Padding="10,2"/>
                        <Button x:Name="CloseButton" Content="×" Padding="10,2"/>
                    </StackPanel>
                </DockPanel>

                <!-- Body -->
                <TextBox x:Name="BodyText"
                         Grid.Row="1"
                         Margin="8,6,8,8"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="White"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>
    </Grid>
</Window>
